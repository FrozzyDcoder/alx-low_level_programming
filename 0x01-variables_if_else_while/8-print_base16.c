#include <stdio.h>
/**
 * main - main block
 * Description: printing all numbers of base 16 in lower case.
 * Return: 0
 */
int main(void)
{
	char x;
	int y;

	for (y = 0; y < 10; y++)
	{
		putchar(y + '0');
	}

	for (x = 'a'; x < 'g'; x++)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
